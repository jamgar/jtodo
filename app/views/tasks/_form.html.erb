<%= simple_form_for(@task) do |f| %>
  <%= f.error_notification %>
  <%= f.input :content, require: true, autofocus: true,  label: false, placeholder: "Content", input_html: { class: "form-control" } %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
